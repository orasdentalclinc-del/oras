# التقرير الأمني — موقع عيادة أوراس لطب الأسنان
**النطاق:** `orasdentalclinic.com` | **التاريخ:** 2026-09-10 | **المرجع:** `orasdentalclinc-del/oras` @ `c52dea4`

> **حالة التقرير:** اكتمل الفحص والتأكيد، ثم نُفِّذت المعالجات المعتمدة (التفاصيل في القسم 7).
> كل نتيجة فحص موسومة بـ ✅ مؤكَّدة (بالتحليل الثابت أو الفحص الحي) أو ⚠️ بحاجة لتحقق يدوي من لوحة التحكم المعنية.

---

## 1) نطاق الفحص ومنهجيته

| المكوّن | التقنية | طريقة التحقق |
|---|---|---|
| الموقع (صفحة واحدة) | HTML/JS ثابت على GitHub Pages | تحليل كود + جلب حي عبر HTTPS ✅ |
| نظام الحجز | Google Apps Script Web App (`booking/Code.gs`) | تحليل كود ✅ (تعذّر الفحص الحي المباشر من بيئة الفحص) |
| المحتوى | Sanity (مشروع `upxb9w10` / dataset `production`) | تحليل كود ✅ |
| المستودع والتاريخ | Git | فحص كامل للتاريخ ✅ |

**قيود الفحص:** بيئة الفحص لا تسمح بالاتصال المباشر (TCP) بخوادم GitHub Pages وSanity من داخلها، لذا تعذّر فحص رؤوس الاستجابة (Headers) مباشرة. تم التحقق من أن الموقع يعمل عبر HTTPS بنجاح عبر منصّة الجلب (وصل المحتوى كاملاً بدون أخطاء شهادة) وأن الدومين يشير لعناوين GitHub Pages (`185.199.x.153`).

---

## 2) الخلاصة

| # | النتيجة | الخطورة | الحالة |
|---|---|---|---|
| 1 | لا توجد أسرار/توكنات مسرّبة في الكود أو تاريخ Git | — | ✅ مؤكَّدة |
| 2 | الموقع يعمل عبر HTTPS بشهادة سليمة | — | ✅ مؤكَّدة |
| 3 | محتوى Sanity يُعرض بطريقة آمنة (لا يوجد مدخل XSS) | — | ✅ مؤكَّدة |
| 4 | ملف `oras-website.zip` منشور للعامة ويحوي `.env` قديم ومعرف مشروع قديم | متوسط | ⚠️ مؤكَّدة منطقيًا (بحاجة لتأكيد الرابط الحي) |
| 5 | واجهة الحجز بلا حد معدّل / كابتشا → إغراق المواعيد ممكن | متوسط | ✅ مؤكَّدة |
| 6 | بيانات المرضى غير المنشورة (التقييمات المعلّقة) قد تكون قابلة للقراءة علنًا عبر Sanity API | متوسط | ⚠️ بحاجة لتحقق من لوحة Sanity |
| 7 | إدراج مدخلات المستخدم في رسائل الخطأ المعروضة بـ `innerHTML` | منخفض | ✅ مؤكَّدة (غير قابلة للاستغلال عمليًا) |
| 8 | غياب رؤوس أمان إضافية (CSP…) — قيد من GitHub Pages | منخفض | ✅ مؤكَّدة |
| 9 | تعارض توثيق: المعرف في README قديم (`j4rqm0i8`) مقابل الفعلي (`upxb9w10`) | معلوماتي | ✅ مؤكَّدة |

**التقييم العام:** الموقع آمن من حيث البنية العامة ولا يحتوي ثغرات حرجة. المخاطر الفعلية تتركز في نقاط التشغيل (إغراق الحجز، خصوصية التقييمات المعلّقة، والملف المضغوط المنشور).

---

## 3) النتائج المؤكَّدة بالتفصيل

### ✅ 3.1 لا أسرار مسرّبة
- فُحصت كل الملفات + تاريخ Git كاملًا (التزام واحد): لا توكنات Sanity، لا مفاتيح Google، لا كلمات مرور.
- الموجود فقط: عناصر نائبة توثيقية مثل `SANITY_WRITE_TOKEN: 'skXXXX'` في `booking/README.md` (مثال للتوضيح، ليس توكن حقيقي).
- توكن الكتابة في Sanity مُصمَّم ليوضع داخل `CONFIG` في Apps Script (جهة الخادم) وليس في كود الموقع — تصميم سليم. في نسخة المستودع الحقل فارغ (`''`)، أي إن النشر الفعلي (إن وُجد التوكن) محفوظ لدى Google وليس هنا.

### ✅ 3.2 النقل والتشفير
- الموقع يُقدَّم عبر HTTPS (تم الجلب الحي بنجاح، بلا أخطاء شهادة).
- الدومين على GitHub Pages مع ملف `CNAME` — GitHub Pages يفرض إعادة توجيه HTTP→HTTPS ويرسل HSTS تلقائيًا للنطاقات المخصصة (سلوك مضمون من المنصة؛ لم يُتح فحص الرؤوس مباشرة من بيئة الفحص).

### ✅ 3.3 سلامة العرض من محتوى لوحة التحكم (XSS)
- `oras-sanity.js` يعرض كل نصوص Sanity عبر `textContent` / `createTextNode` — وليس `innerHTML`.
- استخدامات `innerHTML` محصورة في رسوم SVG ثابتة معرفة داخل الكود (أيقونات/شرائح)، والصور تُعيَّن عبر `setAttribute('src', …)` — لا مسار لحقن أكواد من محتوى اللوحة.

### ✅ 3.4 الروابط الخارجية
- روابط واتساب والخارجية تستخدم `target="_blank"` مع `rel="noopener"` ✅.
- الطرفيات الخارجية الوحيدة: Google Fonts وSanity CDN (`preconnect` + CSS) — قياسية، بدون صلاحيات زائدة.

---

## 4) النتائج التي تحتاج معالجة/تحققًا

### ⚠️ 4.1 `oras-website.zip` منشور للعامة (متوسط)
**الحقائق المؤكدة:** الملف موجود في جذر المستودع (172 كيلوبايت) ويحوي:
- نسخة قديمة كاملة من الموقع تستخدم معرف مشروع Sanity القديم `j4rqm0i8`.
- ملف `studio/.env` (يحوي معرف المشروع القديم فقط — **لا توكنات**، تم فتحه والتأكد).

**الاستنتاج:** لأن GitHub Pages ينشر كل ملفات جذر المستودع، يُتوقع أن الملف متاح على `https://orasdentalclinic.com/oras-website.zip`. لم أتمكن من تأكيد الرابط الحي بسبب قيود بيئة الفحص — **يرجى فتح الرابط للتأكد**.
**الأثر:** كشف بنية النظام + وجود مشروع Sanity قديم (`j4rqm0i8`) يجب فحص إن كان لا يزال قائمًا وعامًا وإلا فحذفه.

### ✅ 4.2 إغراق الحجز والتقييمات (متوسط)
- تطبيق الويب منشور بوصول «أي شخص (مجهول)» وهذا مقصود لعمل الموقع.
- **لكن لا يوجد:** حد معدّل للطلبات، ولا كابتشا، ولا Honeypot.
- الأثر الممكن: أي جهة تستطيع حجز كل السعات (4 لكل ساعة) بأسماء وهمية فتعطّل حجز المرضى الحقيقيين، أو إغراق لوحة التقييمات.
- التخفيف الحالي: `LockService` يمنع التسابق فقط ولا يمنع الإغراق المتعمد.

### ⚠️ 4.3 خصوصية التقييمات المعلّقة في Sanity (متوسط — بحاجة تحقق)
- مسار التقييمات يحفظ `الاسم + الهاتف + التعليق` بحالة `pending` في Sanity (عند ضبط التوكن).
- عمل الموقع يتطلب أن يكون الـ dataset **عام القراءة** (موثّق في `studio/scripts/check.mjs`).
- إن كان الـ dataset عامًا: أي شخص يستطيع قراءة **كل** المستندات عبر GROQ API — بما فيها التقييمات غير المنشورة وما تحويه من أرقام هواتف.
- **لم أتمكن من الاستعلام عن الـ API مباشرة من بيئة الفحص** — يلزم التحقق من لوحة Sanity (Project → Datasets → Visibility) أو بسؤال واحد:
  `https://upxb9w10.api.sanity.io/v2024-01-01/data/query/production?query=*[_type=="review" && status=="pending"]`
- **التوصية لاحقًا (إن ثبت التعرض):** عدم تخزين رقم الهاتف في Sanity إطلاقًا (يبقى في رسالة الواتساب/الإيميل فقط).

### ✅ 4.4 إدراج مدخلات في رسائل الخطأ (منخفض — غير مستغل عمليًا)
- في `Code.gs` تُدمج قيمة `date` كما أدخلها المستخدم داخل نص رسالة الرفض، والموقع يعرض `res.reason` عبر `innerHTML`.
- عمليًا غير قابلة للاستغلال: الرد يعود لنفس صاحب الطلب فقط، وحقل التاريخ في المتصفح `type=date` يقيّد الصيغة. لكنها ثغرة «دفاع في العمق» تُصلح لاحقًا بإسناد الرسائل لنصوص ثابتة أو تهريب المحتوى.

### ✅ 4.5 رؤوس الأمان (منخفض — قيد منصة)
- لا يوجد `meta` CSP في `index.html`، وGitHub Pages لا يدعم رؤوسًا مخصصة. الغياب مقبول نسبيًا لموقع ثابت لا يعالج بيانات حساسة؛ المخالفة الوحيدة الفعلية هي تحميل خط Google الخارجي (قياسي).

### ✅ 4.6 تعارض توثيقي (معلوماتي)
- `README.md` يذكر معرف مشروع `j4rqm0i8` بينما الكود الحي و`Code.gs` يستخدمان `upxb9w10`. التوثيق قديم ويجب تحديثه، مع مراجعة مصير المشروع القديم كما في 4.1.

---

## 5) ما هو سليم ومؤكَّد في الخلفية (الحجوزات)

- التحقق من الحقول الإلزامية ومن نطاق التقييم (1–5) وحدود أطوال النصوص في التقييمات.
- رفض التواريخ الماضية وأيام الجمعة والساعات المنقضية حسب توقيت الخرطوم.
- `LockService` + سعة لكل ساعة — منطق منع التعارض سليم برمجيًا.
- بيانات الحجز (اسم/هاتف/ملاحظات) تُخزن في Google Sheets + Calendar + إيميل — **التأكد المطلوب من جهتك:** أن مشاركة ملف Google Sheets بقيت خاصة (هذا خارج نطاق الفحص التقني من هنا).
- `doGet` يعرض خرائط الإشغال فقط (عدّادات) — لا أي بيانات شخصية ✅.

---

## 5.1) ملحق — إعادة التحقق من صحة التقرير مقابل الواقع (2026-09-10)

أُعيد فحص أدلة التقرير عبر كل القنوات المتاحة، والنتيجة لكل ادعاء:

| الادعاء في التقرير | درجة التأكيد | الدليل |
|---|---|---|
| الموقع يعمل عبر HTTPS بشهادة سليمة | ✅ مؤكَّد حيًا | جَلب ناجح لصفحة البداية عبر منصّة فحص خارجية (المحتوى كاملاً بلا خطأ شهادة) |
| الموقع مستضاف على GitHub Pages بدومين مخصص | ✅ مؤكَّد | DNS: النطاق يؤول إلى عناوين GitHub Pages الأربعة `185.199.108-111.153` + ملف `CNAME=orasdentalclinic.com` مؤكد عبر GitHub API |
| المحتوى يُخدم من Sanity مشروع `upxb9w10` | ✅ مؤكَّد حيًا | كل صور الصفحة المجلوبة من `cdn.sanity.io/images/upxb9w10/...` |
| لا أسرار في الكود ولا في تاريخ Git | ✅ مؤكَّد (فحص شامل) | تحليل كامل للمستودع والتاريخ |
| لا توجد ثغرة XSS من محتوى اللوحة | ✅ مؤكَّد (تحليل ثابت) | كل النصوص عبر `textContent`/`createTextNode` |
| `oras-website.zip` موجود في جذر المستودع العام ويحوي `.env` قديم | ✅ مؤكَّد | تحقق عبر GitHub API: الملف موجود بحجم 172,906 بايت وبصمة مطابقة للنسخة المحلية، والمستودع عام — وفتح الملف أكد وجود `studio/.env` (معرف مشروع فقط، بلا توكنات) |
| الملف متاح فعليًا على رابط الموقع الحي | ⚠️ شبه مؤكَّد (منطقيًا) | GitHub Pages ينشر كل ملفات الجذر بلا استثناء، لكن تعذّر فتح الرابط مباشرة من بيئة الفحص |
| الـ dataset عام (وبيانات التقييمات المعلّقة قابلة للقراءة) | ⚠️ مرجَّح ولم يُحسم | عمل الموقع نفسه يثبت أن القراءة عامة، لكن استعلام الـ API المباشر عن المستندات المعلّقة تعذّر |
| رؤوس الأمان (HSTS وغيره) | ⚠️ غير مفحوصة مباشرة | تعذّر قراءة الرؤوس من بيئة الفحص — سلوك افتراضي متوقع من GitHub Pages |

**القيود التقنية وقت إعادة الفحص:** الاتصال المباشر من بيئة الفحص (TCP/443) محجوب نحو عناوين خارج النطاق المسموح، ومنصّة الجلب الخارجية تعطلت بعد الجلب الأول الناجح؛ استُعيض عنها بـ GitHub API وDNS، وكلاهما عملا.

---

## 7) المعالجات المنفَّذة (2026-09-10)

بعد اعتماد المستخدم، نُفِّذت الإصلاحات التالية في الفرع `arena/01a08a43-oras`:

| # | المشكلة | المعالجة | الملفات |
|---|---|---|---|
| 1 | `oras-website.zip` المنشور وفيه `.env` قديم ومعرف مشروع قديم | **حُذف نهائياً من المستودع** — لن يعود متاحاً على الموقع بعد النشر القادم | `oras-website.zip` (حذف) |
| 2 | الحجز والتقييم بلا حماية من الروبوتات | **حقل مصيدة (Honeypot)** مخفي في نموذجي الحجز والتقييم + تجاهل أي طلب يملؤه على الخادم بردّ نجاح وهمي | `index.html`، `oras-booking.js`، `booking/Code.gs` |
| 3 | أرقام هواتف المقيّمين قابلة للتخزين في بيانات عامة القراءة | **أُوقف تخزين الهاتف في Sanity نهائياً** — التقييم يُحفظ بالاسم والتعليق فقط، مع تعليق أمني في الكود وتوضيح في مخطط الحقل | `booking/Code.gs`، `studio/schemaTypes/review.js` |
| 4 | صدى مدخلات المستخدم في رسائل الخطأ | تحقق صارم من صيغة التاريخ (رفض أي نسق غير `YYYY-MM-DD`) + حدود أطوال لكل الحقول (الاسم 120، الهاتف 40، الخدمة 120، الملاحظات 1000) + تهريب `escapeHtml` في الواجهة لأي نص قادم من الخادم أو مدخل يُعرض داخل HTML | `booking/Code.gs`، `oras-booking.js` |
| 5 | المعرف القديم في التوثيق | حُدِّث إلى `upxb9w10` في كل المواضع | `README.md` |
| 6 | كاش المتصفحات | رُفعت إصدارات السكربتات (`?v=12` / `?v=4`) ليصل التحديث للزوار فور النشر | `index.html` |

**التحقق:** فحص صياغة برمجي ناجح لكل الملفات المعدَّلة (`node --check` + فحص السكربتات المضمّنة في `index.html`).

### ⚠️ خطوات مطلوبة من صاحب الموقع (لا تتم من هذا المستودع)
1. **دمج/نشر التعديلات** حتى يتحدث الموقع الحي (النشر على GitHub Pages تلقائي بعد الدمج في `main`).
2. **تحديث سكربت الحجز المنشور في Google Apps Script**: انسخ محتوى `booking/Code.gs` الجديد ثم `Deploy → Manage deployments → Edit → Version: New version → Deploy` (بدون هذه الخطوة المصيدة والتحققات الجديدة لا تعمل على الخادم).
3. **التحقق من مشروع Sanity القديم `j4rqm0i8`** في https://sanity.io/manage — إن كان لا يزال قائماً وبه بيانات فاحذفه أو أغلق قراءته العامة.
4. (اختياري لاحقاً) كابتشا حقيقية مثل Turnstile إن ظهر إزعاج ممنهج يتجاوز المصيدة.

## 8) الخلاصة النهائية

نُفِّذت كل المعالجات ذات الأولوية المذكورة أعلاه في الكود والتوثيق (انظر القسم 7)، وتبقى ثلاث نقاط تشغيلية بيد صاحب الموقع: نشر التعديلات، إعادة نشر سكربت الحجز في Google Apps Script، ومراجعة مصير مشروع Sanity القديم `j4rqm0i8`.

بعد إتمام هذه الخطوات يصبح وضع الموقع:
- ✅ بلا ملفات منشورة تحوي إعدادات قديمة.
- ✅ محميًا من الإزعاج الآلي البسيط (مصيدة + تحقق صيغة + حدود أطوال).
- ✅ بلا تخزين لأي أرقام هواتف في بيانات عامة القراءة.
- ✅ بلا صدى لمدخلات المستخدم في أي سياق HTML.
